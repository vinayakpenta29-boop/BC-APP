// Root-level build.gradle (Gradle 8.x + AGP 8.1.1 compliant)

buildscript {
    dependencies {
        classpath "com.android.tools.build:gradle:8.1.1"
    }
}

// Clean task
tasks.register("clean", Delete) {
    delete rootProject.buildDir
}
